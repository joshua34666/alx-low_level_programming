#include "main.h"
/**
 * main - check for the code
 *
 * Return: Always 0.
 */
int main(void)
{
	char *str;

	str = "I do not fear computers. i fear the lack of them - Isaac Asimov";
	print_rev(str);
	return (0);
}
