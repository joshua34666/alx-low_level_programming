#include "main.h"
/**
 * add - a function that adds two integers and returns the result.
 *
 * @a: number one.
 * @b: number two.
 *
 * Return: Add the two numbers.
 */
int add(int a, int b)
{
	return (a + b);
}
